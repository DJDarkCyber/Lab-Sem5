# Ex.No.17 - Simulation of Distance Vector Routing algorithm for finding the shortest-path routing.

```
$ ns sim.tcl
```

Execution :

1. Run `ns sim.tcl` in terminal.

OUTPUT:

`ns sim.tcl`

![image](image.png)